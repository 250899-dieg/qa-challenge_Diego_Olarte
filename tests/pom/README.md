# Page Object Model Workspace

Candidates should place their Playwright Page Objects in this directory.

Suggested structure:

```
tests/pom/
├── home.page.ts
├── results.page.ts
└── api-client.ts
```

Document defects discovered via Page Objects inside `BUGS.md` per the README instructions.
